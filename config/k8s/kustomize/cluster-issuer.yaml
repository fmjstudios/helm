apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  labels:
    adnoctem.co/component: cert-manager
    adnoctem.co/env: dev
  name: development-issuer
  namespace: cert-manager
spec:
  ca:
    secretName: root-ca
