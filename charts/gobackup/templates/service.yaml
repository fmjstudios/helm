apiVersion: v1
kind: Service
metadata:
  name: {{ include "gobackup.fullname" . }}
  labels:
    {{- include "gobackup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gobackup.selectorLabels" . | nindent 4 }}
