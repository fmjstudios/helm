apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gobackup.fullname" . }}-test-connection"
  labels:
    {{- include "gobackup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gobackup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
