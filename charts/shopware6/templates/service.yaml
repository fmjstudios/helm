apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopware6.fullname" . }}
  labels:
    {{- include "shopware6.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shopware6.selectorLabels" . | nindent 4 }}
